---
ansible-navigator:
  execution-environment:
    image: registry.gitlab-bs.demolab.ch:4567/ansible-ee/ansible_ee_vmware:latest
    pull:
      policy: always
    environment-variables:
      set:
        KUBECONFIG: /home/runner/.kube/rke2.yaml
    volume-mounts:
      - src: "/home/dago/.kube/"
        dest: "/home/runner/.kube"
