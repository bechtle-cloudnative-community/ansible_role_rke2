---
# vars file for rke2
rke2_update: false

# only needed when rke2_extra_container path is defined
rke2_dest_containderd: "{{ rke2_data_dir }}/agent"
rke2_containerd_subpath:
  - "{{ rke2_extra_container_path }}"
  - "{{ rke2_extra_image_path }}"
